{
  "name": "flyer-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local MCP bridge: relays Claude tool calls to the live flyer-editor tab over wss. Never deployed.",
  "bin": {
    "flyer-bridge": "server.js"
  },
  "scripts": {
    "start": "node server.js",
    "relay": "node relay.js",
    "setup": "node setup.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ws": "^8.18.0"
  }
}
